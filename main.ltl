// ltl-ascii-art/main.ltl
//
// Image-to-ASCII converter with dithering.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-ascii-art v0.1.0")
    io::println("Image-to-ASCII converter with dithering.")
    0
}
