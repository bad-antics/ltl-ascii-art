// examples/demo.ltl — runnable demo for ltl_ascii_art

import std::io
use ltl_ascii_art as lib

fn main() {
    io::println("ltl_ascii_art demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}